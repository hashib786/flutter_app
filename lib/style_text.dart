import 'package:flutter/material.dart';

class StyleTextNew extends StatelessWidget {
  const StyleTextNew({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Hashib Raja3",
        style: TextStyle(
          color: Colors.white,
          fontSize: 28,
          fontWeight: FontWeight.bold,
        ));
  }
}
